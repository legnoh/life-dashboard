all:
  vars:
    ansible_python_interpreter: /opt/homebrew/Cellar/ansible/11.2.0/libexec/bin/python
    home_dir: "{{ ansible_env.HOME }}"

grafstation:
  hosts:
    localhost:
      ansible_connection: local
      ansible_host: localhost
