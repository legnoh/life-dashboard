SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

# Check if wifi is connected
* * * * *  root reload-wifi >> /dev/null 2>&1

# reload grafana-kiosk
0 7,12,13,16,19 * * * root systemctl restart grafana-kiosk >> /dev/null 2>&1
