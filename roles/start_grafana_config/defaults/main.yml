---

start_grafana_config_abema_jwt_token: "{{ lookup('env', 'ABEMA_JWT_TOKEN', default=Undefined) }}"
start_grafana_config_grafana_admin_password: "{{ lookup('env', 'GRAFANA_ADMIN_PASSWORD', default=Undefined) }}"
start_grafana_config_greench_email: "{{ lookup('env', 'GREENCH_EMAIL', default=Undefined) }}"
start_grafana_config_greench_password: "{{ lookup('env', 'GREENCH_PASSWORD', default=Undefined) }}"
start_grafana_config_nature_remo_device_name: "{{ lookup('env', 'NATURE_DEVICE_NAME', default=Undefined) }}"
start_grafana_config_openweather_city: "{{ lookup('env', 'OPENWEATHER_CITY', default=Undefined) }}"
start_grafana_config_youtube_playlist_id: "{{ lookup('env', 'YOUTUBE_PLAYLIST_ID', default=Undefined) }}"
